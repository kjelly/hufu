# session-hufu-local

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

A session-only task has a bare model and the historical `hufu-local` provider
shadow. The materializer must derive exactly one `ollama` target without
consulting current configuration.
