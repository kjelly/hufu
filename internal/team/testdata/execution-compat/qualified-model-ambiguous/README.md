# qualified-model-ambiguous

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

Case 7: a qualified legacy model has no durable backend evidence. Migration
must fail closed rather than consult current provider configuration.
