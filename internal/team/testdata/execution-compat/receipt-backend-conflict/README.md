# receipt-backend-conflict

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

Case 10: a receipt's canonical backend conflicts with its legacy provider.
The contradiction is ambiguous and must never be repaired by guessing.
