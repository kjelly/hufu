# canonical-current

Producer: canonical fixture builder v1.

This workspace contains a current typed task and is expected to produce zero
compatibility findings. It proves the scanner does not create false positives
for an already canonical session-only projection.
