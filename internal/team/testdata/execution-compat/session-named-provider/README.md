# session-named-provider

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

Case 3: a target-less session task records a named historical
`SubagentProvider`. The materializer must preserve that backend identity.
