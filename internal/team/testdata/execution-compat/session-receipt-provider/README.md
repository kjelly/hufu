# session-receipt-provider

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

The execution receipt is the remaining legacy provider-bearing field. Its
backend must be materialized as `ollama` while the task keeps one unique
historical execution identity.
