# session-provider-binding

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

Case 4: a target-less qualified model has a durable historical
`ProviderBinding`, from which one canonical backend can be recovered.
