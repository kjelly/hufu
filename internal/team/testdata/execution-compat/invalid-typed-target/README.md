# invalid-typed-target

Producer: fixed legacy fixture builder v1 (pre canonical execution identity).

Case 9: a typed target is structurally incomplete. It is unmigratable and an
apply attempt must append nothing.
