apiVersion: hufu.io/v1alpha1
kind: AgentTeam
metadata:
  name: hufu-code-review
spec:
  description: "Read-only, artifact-backed review of the Hufu Go codebase"
  max-rounds: 64
  max-steps: 120
  timeout: 3600
  verify-timeout: 120
  max-retries: 1
  max-concurrent: 3
  workspace: workspace
  goal-mode: outcome
  execution-profile: fresh-session
  unattended: true
  max-duration: 86400
  max-total-tokens: 0
  no-net: true
  allow-free-text-results: false

  inputs:
    review.scope:
      type: object
      required: true
      default:
        kind: last_n
        count: 10
        history: first_parent
        head: HEAD
      properties:
        kind:
          type: string
          enum: [last_n, revision_range, since]
        count:
          type: integer
          minimum: 1
          maximum: 100
        history:
          type: string
          enum: [first_parent]
        head:
          type: string
          min-length: 1
          max-length: 160
        base:
          type: string
          max-length: 160
        since:
          type: string
          max-length: 80
      required-properties: [kind, history, head]
      additional-properties: false
      resolver:
        id: review-scope-v1
        capability: resolve-review-scope
        type: resolve_review_scope
        source: invocation_prompt
        side_effect: none
        timeout: 10

  subagent-providers:
    codex:
      type: codex-app-server
      command: [codex, app-server]
      startup-timeout: "30s"
      interrupt-grace: "5s"
      shutdown-grace: "5s"
      max-event-bytes: 16777216
      max-transcript-bytes: 16777216
      max-concurrent: 1
      inherit-env: [PATH, CODEX_HOME, HOME]

  workflow:
    phases: [prepare, verify]
  policies:
    require_phase_success: true
    allow_phase_skip: true
    fail_fast: true
  capabilities:
    required: [produce-workset]
  verification:
    required: true
  retry:
    transient:
      max_attempts: 1
    repair:
      max_attempts_per_failure_signature: 1

  action-providers:
    resolve-review-scope:
      command: [go, run, ./.agent-teams/hufu-code-review/reviewprep]
      dir: .
      timeout: 30
    produce-workset:
      command: [go, run, ./.agent-teams/hufu-code-review/reviewprep]
      dir: .
      timeout: 300

  delegation:
    bind-task-goal-contracts: true
    allowed-workers: [reviewer, critic]
    no-redispatch-after-success: [reviewer, critic]

  acceptance:
    mode: blocking
    require-no-unresolved-tasks: true
    verifications:
      - type: task_output_assert
        source-task: produce-workset
        output: scope
        assertions:
          - { pointer: /requested, op: equals_input, input: review.scope }
          - { pointer: /requested_input_hash, op: equals_input_hash, input: review.scope }
          - { pointer: /satisfied, op: equals, value: true }
          - { pointer: /resolved/selected_commit_count, op: minimum, value: 1 }
      - type: workset_complete
        source-task: review-workset
        require-all-terminal: true
        require-all-verified: true
        accepted-statuses: [success, completed_with_gaps]

  # The producer is deterministic. The optional critic is authorized in VERIFY
  # but does not block a clean review.
  tasks:
    - id: produce-workset
      agent: reviewer
      when-goal-contains: produce workset
      phase: prepare
      side_effect: none
      recovery: retry
      action:
        capability: produce-workset
        type: prepare_review_workset
        payload: >-
          {"scope":{"kind":"last_n","count":10,"history":"first_parent","head":"HEAD"},"max_total_diff_bytes":2097152,"max_total_diff_lines":30000,"max_changed_paths":256,"max_workset_items":32,"max_diff_bytes":96000,"max_diff_lines":2000,"max_paths":32}
        input-bindings:
          - input: review.scope
            target: /scope

    - id: review-workset
      agent: reviewer
      when-goal-contains: review workset
      phase: verify
      invariant-verification: report
      fan_out:
        source-artifact:
          task_id: produce-workset
          artifact: workset manifest
        goal-template: "Review workset item {key} with the {lens} lens."
      execution:
        requires-result: true
        requires-grounded-result: true
        forbid-artifacts: true
      verify-spec:
        type: task_result_assert
        task-result-assertions:
          - { pointer: /summary, op: non_empty }
          - { pointer: /files_read, op: min_items, value: 1 }

    - id: critic-review
      agent: critic
      when-goal-contains: critic review
      phase: verify
      optional: true
      execution:
        requires-result: true
        requires-grounded-result: true
      verify-spec:
        type: task_result_assert
        task-result-assertions:
          - { pointer: /summary, op: non_empty }
