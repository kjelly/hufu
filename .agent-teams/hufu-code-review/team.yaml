apiVersion: hufu.io/v1alpha1
kind: AgentTeam
metadata:
  name: hufu-code-review
spec:
  description: "Read-only, artifact-backed review of the Hufu Go codebase"
  coordinator-model: ollama/qwen3.5:cloud
  max-rounds: 64
  max-steps: 120
  timeout: 3600
  verify-timeout: 120
  max-retries: 1
  max-concurrent: 3
  workspace: workspace
  goal-mode: outcome
  execution-profile: fresh-session
  unattended: true
  # This team is read-only. No rollback command is configured: acceptance
  # failure must fail closed without modifying the repository.
  max-duration: 86400
  max-total-tokens: 0
  no-net: true
  allow-free-text-results: false

  inputs:
    review.scope:
      type: object
      required: true
      default:
        kind: last_n
        count: 10
        history: first_parent
        head: HEAD
      properties:
        kind:
          type: string
          enum: [last_n, revision_range, since, working_tree]
        count:
          type: integer
          minimum: 1
          maximum: 100
        history:
          type: string
          enum: [first_parent]
        head:
          type: string
          min-length: 1
          max-length: 160
        base:
          type: string
          max-length: 160
        since:
          type: string
          max-length: 80
      required-properties: [kind, history, head]
      additional-properties: false
      resolver:
        id: review-scope-v1
        capability: resolve-review-scope
        type: resolve_review_scope
        mode: semantic_json
        source: invocation_prompt
        side_effect: none
        timeout: 10

  subagent-providers:
    codex:
      type: codex-app-server
      command: [codex, app-server]
      startup-timeout: "30s"
      interrupt-grace: "5s"
      shutdown-grace: "5s"
      max-event-bytes: 16777216
      max-transcript-bytes: 16777216
      max-concurrent: 1
      inherit-env: [PATH, CODEX_HOME, HOME]

  workflow:
    phases: [prepare, verify]
  policies:
    require_phase_success: true
    allow_phase_skip: true
    fail_fast: true
  capabilities:
    required: [produce-workset]
  verification:
    required: true
  retry:
    transient:
      max_attempts: 1
    repair:
      max_attempts_per_failure_signature: 1

  action-providers:
    resolve-review-scope:
      runtime: golang
      source: ./reviewprep
      mode: trusted-static
      timeout: 30
    produce-workset:
      runtime: golang
      source: ./reviewprep
      mode: trusted-static
      timeout: 300

  delegation:
    bind-task-goal-contracts: true
    allowed-workers: [reviewer, documentation-reviewer, critic]
    no-redispatch-after-success: [reviewer, documentation-reviewer]

  acceptance:
    mode: blocking
    require-no-unresolved-tasks: true
    verifications:
      - type: task_output_assert
        source-task: produce-workset
        output: scope
        assertions:
          - { pointer: /requested, op: equals_input, input: review.scope }
          - { pointer: /requested_input_hash, op: equals_input_hash, input: review.scope }
          - { pointer: /satisfied, op: equals, value: true }
          - { pointer: /observed_budget/changed_paths, op: minimum, value: 1 }
      - type: task_output_assert
        source-task: produce-workset
        output: documentation_verification
        assertions:
          - { pointer: /passed, op: equals, value: true }
      - type: workset_complete
        source-task: review-primary-workset
        require-all-terminal: true
        require-all-verified: true
        accepted-statuses: [success, completed_with_gaps]
      - type: workset_complete
        source-task: review-documentation-workset
        require-all-terminal: true
        require-all-verified: true
        accepted-statuses: [success, completed_with_gaps]
      - type: workset_complete
        source-task: review-documentation-escalation
        require-all-terminal: true
        require-all-verified: true
        accepted-statuses: [success, completed_with_gaps]

  # The producer and all three routed worksets are blocking. A second critic
  # pass over a typed primary finding remains optional.
  tasks:
    - id: produce-workset
      agent: reviewer
      when-goal-contains: produce workset
      phase: prepare
      side_effect: none
      recovery: retry
      action:
        capability: produce-workset
        type: prepare_review_workset
        payload: >-
          {"scope":{"kind":"last_n","count":10,"history":"first_parent","head":"HEAD"},"routing":"documentation","max_total_diff_bytes":2097152,"max_total_diff_lines":30000,"max_changed_paths":256,"max_workset_items":32,"max_diff_bytes":96000,"max_diff_lines":2000,"max_paths":32}
        input-bindings:
          - input: review.scope
            target: /scope

    - id: review-primary-workset
      agent: reviewer
      when-goal-contains: review primary workset
      phase: verify
      invariant-verification: report
      fan_out:
        source-artifact:
          task_id: produce-workset
          artifact: primary review workset manifest
        goal-template: "Review primary workset item {key} with the {lens} lens."
      execution:
        requires-result: true
        requires-grounded-result: true
        forbid-artifacts: true
      verify-spec:
        type: task_result_assert
        task-result-assertions:
          - { pointer: /summary, op: non_empty }
          - { pointer: /files_read, op: min_items, value: 1 }

    - id: review-documentation-workset
      agent: documentation-reviewer
      when-goal-contains: review documentation workset
      phase: verify
      fan_out:
        source-artifact:
          task_id: produce-workset
          artifact: documentation review workset manifest
        goal-template: "Review routine documentation workset item {key} with the {lens} lens."
      execution:
        requires-result: true
        requires-grounded-result: true
        forbid-artifacts: true
      verify-spec:
        type: task_result_assert
        task-result-assertions:
          - { pointer: /summary, op: non_empty }
          - { pointer: /files_read, op: min_items, value: 1 }

    - id: review-documentation-escalation
      agent: critic
      when-goal-contains: review documentation escalation
      phase: verify
      fan_out:
        source-artifact:
          task_id: produce-workset
          artifact: documentation escalation workset manifest
        goal-template: "Adversarially review escalated documentation workset item {key} with the {lens} lens."
      execution:
        requires-result: true
        requires-grounded-result: true
        forbid-artifacts: true
      verify-spec:
        type: task_result_assert
        task-result-assertions:
          - { pointer: /summary, op: non_empty }
          - { pointer: /files_read, op: min_items, value: 1 }

    - id: critic-review
      agent: critic
      when-goal-contains: critic review
      phase: verify
      optional: true
      execution:
        requires-result: true
        requires-grounded-result: true
      verify-spec:
        type: task_result_assert
        task-result-assertions:
          - { pointer: /summary, op: non_empty }
